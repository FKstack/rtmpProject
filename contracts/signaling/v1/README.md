# Signaling contract v1 vectors

These files are offline protocol fixtures shared by the C++ contract tests and the Go provisioning tests. They contain
symbolic identities and documentation-only addresses. They must never contain credentials, real endpoints, SDP from a
real session, MQTT wildcards, or legacy `device/control` and `device/status` topics.
